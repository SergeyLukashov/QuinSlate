<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="QuinSlate.Ui.Components.TabEditFlyoutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QuinSlate.Ui.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <x:Double x:Key="RootSpacing">8</x:Double>
        <x:Double x:Key="EmojiButtonSize">28</x:Double>
        <x:Double x:Key="EmojiButtonFontSize">16</x:Double>
        <x:Double x:Key="TitleFieldWidth">140</x:Double>
        <x:Double x:Key="ControlHeight">28</x:Double>
        <x:Double x:Key="TitleFontSize">13</x:Double>
        <x:Double x:Key="ButtonFontSize">12</x:Double>

        <Thickness x:Key="EmojiButtonPadding">0,0,0,1</Thickness>
        <Thickness x:Key="TitleBoxPadding">4,4,4,0</Thickness>
        <Thickness x:Key="CancelButtonPadding">8,0</Thickness>
        <Thickness x:Key="SaveButtonPadding">12,0</Thickness>

        <!--
            FlatAccentButtonStyle is a minimal accent button template that omits the
            ContentPresenter.BackgroundTransition present in the stock AccentButtonStyle.
            That implicit BrushTransition, when interrupted by a medium-speed pointer crossing,
            makes the compositor paint a one-frame black flash over the bright accent fill.
            Switching the background/border/foreground instantly via VisualState.Setters keeps
            the standard accent feedback while leaving no transition to interrupt.
        -->
        <Style x:Key="FlatAccentButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{ThemeResource AccentButtonBackground}" />
            <Setter Property="BackgroundSizing" Value="OuterBorderEdge" />
            <Setter Property="BorderBrush" Value="{ThemeResource AccentButtonBorderBrush}" />
            <Setter Property="Foreground" Value="{ThemeResource AccentButtonForeground}" />
            <Setter Property="BorderThickness" Value="{ThemeResource ButtonBorderThemeThickness}" />
            <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}" />
            <Setter Property="Padding" Value="{StaticResource SaveButtonPadding}" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}" />
            <Setter Property="FocusVisualMargin" Value="-3" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter
                            x:Name="ContentPresenter"
                            Background="{TemplateBinding Background}"
                            BackgroundSizing="{TemplateBinding BackgroundSizing}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            ContentTransitions="{TemplateBinding ContentTransitions}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            Foreground="{TemplateBinding Foreground}"
                            Padding="{TemplateBinding Padding}"
                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                            AutomationProperties.AccessibilityView="Raw">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenter.Background" Value="{ThemeResource AccentButtonBackgroundPointerOver}" />
                                            <Setter Target="ContentPresenter.BorderBrush" Value="{ThemeResource AccentButtonBorderBrushPointerOver}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource AccentButtonForegroundPointerOver}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenter.Background" Value="{ThemeResource AccentButtonBackgroundPressed}" />
                                            <Setter Target="ContentPresenter.BorderBrush" Value="{ThemeResource AccentButtonBorderBrushPressed}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource AccentButtonForegroundPressed}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenter.Background" Value="{ThemeResource AccentButtonBackgroundDisabled}" />
                                            <Setter Target="ContentPresenter.BorderBrush" Value="{ThemeResource AccentButtonBorderBrushDisabled}" />
                                            <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource AccentButtonForegroundDisabled}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </ContentPresenter>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel
        x:Name="ContentRoot"
        Spacing="{StaticResource RootSpacing}">

        <StackPanel
            Orientation="Horizontal"
            VerticalAlignment="Stretch"
            Spacing="{StaticResource RootSpacing}">
            <Button
                x:Name="EmojiPickerButton"
                Width="{StaticResource EmojiButtonSize}"
                Height="{StaticResource EmojiButtonSize}"
                FontSize="{StaticResource EmojiButtonFontSize}"
                CornerRadius="{ThemeResource ControlCornerRadius}"
                Padding="{StaticResource EmojiButtonPadding}"
                HorizontalContentAlignment="Center"
                VerticalContentAlignment="Center"
                Click="OnEmojiButtonClick" />
            <TextBox
                x:Name="TitleBox"
                Width="{StaticResource TitleFieldWidth}"
                Height="{StaticResource ControlHeight}"
                MinHeight="0"
                Padding="{StaticResource TitleBoxPadding}"
                PlaceholderText="Tab title"
                FontSize="{StaticResource TitleFontSize}"
                MaxLength="12"
                VerticalAlignment="Center"
                VerticalContentAlignment="Center"
                Style="{StaticResource CompactClearTextBoxStyle}" />
        </StackPanel>

        <StackPanel
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Spacing="{StaticResource RootSpacing}">
            <Button
                x:Name="CancelButton"
                Content="Cancel"
                Height="{StaticResource ControlHeight}"
                Padding="{StaticResource CancelButtonPadding}"
                CornerRadius="{ThemeResource ControlCornerRadius}"
                FontSize="{StaticResource ButtonFontSize}"
                VerticalAlignment="Center"
                Click="OnCancelClick" />
            <Button
                x:Name="SaveButton"
                Content="Save"
                Height="{StaticResource ControlHeight}"
                Padding="{StaticResource SaveButtonPadding}"
                FontSize="{StaticResource ButtonFontSize}"
                Style="{StaticResource FlatAccentButtonStyle}"
                Click="OnSaveClick" />
        </StackPanel>

    </StackPanel>
</UserControl>
